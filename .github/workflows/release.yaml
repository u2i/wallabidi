name: Release
on:
  push:
    branches:
      - main
  workflow_dispatch: {}

permissions:
  contents: write
  pull-requests: write

jobs:
  release:
    name: release
    runs-on: ubuntu-latest
    strategy:
      matrix:
        otp: [27.x]
        elixir: [1.18.x]
    steps:
      - uses: googleapis/release-please-action@v5
        id: release

      - uses: actions/checkout@v7
        if: ${{ steps.release.outputs.release_created || github.event_name == 'workflow_dispatch' }}

      - uses: erlef/setup-beam@v1
        id: install
        with:
          otp-version: ${{matrix.otp}}
          elixir-version: ${{matrix.elixir}}
        if: ${{ steps.release.outputs.release_created || github.event_name == 'workflow_dispatch' }}

      - uses: actions/cache@v6
        id: cache
        if: ${{ steps.release.outputs.release_created || github.event_name == 'workflow_dispatch' }}
        with:
          path: |
            deps
            _build
          key: ${{ runner.os }}-mix-${{steps.install.outputs.otp-version}}-${{steps.install.outputs.elixir-version}}-${{ hashFiles('**/mix.lock') }}
          restore-keys: |
            ${{ runner.os }}-mix-${{steps.install.outputs.otp-version}}-${{steps.install.outputs.elixir-version}}-

      - name: Install Dependencies
        if: (steps.release.outputs.release_created || github.event_name == 'workflow_dispatch') && steps.cache.outputs.cache-hit != 'true'
        run: mix deps.get

      - name: publish to hex
        if: ${{ steps.release.outputs.release_created || github.event_name == 'workflow_dispatch' }}
        env:
          HEX_API_KEY: ${{secrets.HEX_API_KEY}}
        run: |
          mix hex.publish --yes
